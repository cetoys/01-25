# Terraform Templates

> Placeholder: Define infrastructure modules for provisioning a single VM running Traefik/Nginx, NestJS API, Postgres, Redis, MinIO, and PostHog. Include variables for domain, TLS, and secrets.
